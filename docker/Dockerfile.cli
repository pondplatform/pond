FROM golang:1.25-alpine AS builder
WORKDIR /src
COPY go.mod go.sum ./
RUN go mod download
COPY . .
RUN CGO_ENABLED=0 go build -o /pond-cli ./cmd/pond-cli

FROM alpine:3.22
RUN apk add --no-cache ca-certificates
COPY --from=builder /pond-cli /usr/local/bin/pond
ENTRYPOINT ["pond"]
